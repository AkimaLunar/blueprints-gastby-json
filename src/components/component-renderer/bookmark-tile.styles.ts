import { makeStyles } from "@griffel/react";

export const useBookmarkTileComponentStyles = makeStyles({
    root: {
      maxWidth: '316px',
      display: 'inline-flex',
      alignItems: 'unset',
    },
  });